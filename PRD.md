# 냉장고 재료 인식 레시피 추천 서비스 PRD

## 1. 목표와 성공조건

**목표**
식재료 사진을 첨부하고 보유한 조미료를 버튼으로 선택하면, 난이도·소요시간·칼로리와 함께 레시피 순서를 1~3개 추천해주는 단일 화면 서비스를 만든다.

**성공조건**
- 사진 첨부 + 조미료 선택 → 요약 버튼 클릭 → 레시피 결과가 화면에 표시된다.
- 전체 플로우(입력 → 처리 → 결과)가 화면 이동/새로고침 없이 하나의 화면 안에서 완결된다.
- 예외 상황(인식 실패, 응답 지연, 응답 실패 등)에서도 화면이 멈추거나 깨지지 않는다.

---

## 2. 사용자 스토리

1. 냉장고에 애매하게 재료가 남아있는 자취생으로서, 일일이 유튜브를 찾아보는 번거로움 없이 간편하게 요리를 하고 싶다.
2. 칼로리를 따지는 사람으로서, 식단 관리를 위해 있는 재료로 낮은 칼로리 요리를 간편하게 만들고 싶다.

---

## 3. 화면 명세 (단일 화면, 3개 영역)

### 3-1. 입력 영역
- 사진 첨부 버튼: "식재료 사진을 찍으세요" 안내 문구 표시
- 조미료 선택 버튼 그룹: 여러 조미료 버튼을 나열, "조미료를 클릭해주세요" 안내 문구 표시 (다중 선택 가능, 토글 방식)
- 안내 문구 영역: 사진 인식이 잘 안 될 경우 "재촬영" 텍스트를 **빨간색**으로 표시

### 3-2. 요약(액션) 버튼
- 위치: 입력 영역 하단
- 클릭 시 동작: 사진 인식 완료 여부 + 조미료 선택 여부를 확인 → 버튼이 로딩 상태로 전환 → 결과 영역으로 화면 스크롤/전환

### 3-3. 결과 영역
- 레시피 카드 1~3개, 각 카드 상단에 난이도 / 소요시간 / 칼로리 표시
- 카드 본문에 조리 순서(단계별 텍스트) 표시
- 레시피에 사용자가 보유하지 않은 재료가 포함된 경우, 해당 재료명을 **빨간색 글씨**로 표시

---

## 4. 기능 명세

**입력**
- 사용자가 식재료 사진을 1장 첨부한다.
- 사용자가 조미료 버튼 목록에서 보유한 조미료를 선택(다중 선택 가능)한다.

**검증**
- 첨부된 사진에서 식재료가 정상적으로 인식되었는지 확인한다.
- 인식된 식재료(+선택된 조미료)로 레시피를 구성할 수 있는지 확인한다.

**처리**
- 인식된 식재료 목록과 선택된 조미료 목록을 AI에 전달하여 레시피 추천을 요청한다.

**출력**
- 난이도, 소요시간, 칼로리, 조리 순서를 포함한 레시피를 1~3개 표시한다.
- 추천 레시피에 사용자가 보유하지 않은 재료가 있으면 해당 재료를 빨간색 글씨로 명시한다.

---

## 5. 예외 처리 (상세)

### 5-1. 사진 인식 실패 (재료를 인식하지 못함)
- **발생 조건**: 사진 업로드 후 이미지 인식 결과가 비어있거나, 신뢰도가 낮아 유효한 재료를 추출하지 못한 경우
- **화면 반응**: 입력 영역의 "재촬영" 문구를 빨간색으로 표시. 요약 버튼은 비활성화 상태를 유지(또는 클릭 시 동일 안내 재노출)하여 다음 단계로 진행되지 않도록 한다
- **사용자 행동 유도**: 사진을 다시 촬영/첨부하도록 유도. 텍스트로 재료를 직접 수정하는 기능은 제공하지 않는다 (재촬영만 허용)
- **복구 조건**: 재촬영한 사진에서 재료가 정상 인식되면 빨간색 문구는 사라지고 요약 버튼이 다시 활성화된다

### 5-2. 조미료 미선택 상태로 요약 버튼 클릭
- **발생 조건**: 조미료를 하나도 선택하지 않고 요약 버튼을 클릭한 경우
- **화면 반응**: "조미료를 클릭해주세요" 안내 문구를 강조 표시(예: 색상 변경 또는 포커스 이동)하고, 결과 영역으로는 진행하지 않는다
- **참고**: 조미료를 아예 선택하지 않아도 진행 자체는 허용할지, 필수로 최소 1개를 강제할지는 정책 결정 필요 — 본 PRD에서는 최소 0개도 허용하되, 아무것도 선택하지 않았을 때는 재확인 안내만 1회 노출하는 것을 기본값으로 한다

### 5-3. 레시피 내 재료 부족(없는 재료 포함)
- **발생 조건**: AI가 추천한 레시피에 사용자가 보유하지 않은(사진에서 인식되지 않은, 조미료 목록에도 없는) 재료가 포함된 경우
- **화면 반응**: 결과 카드 내 해당 재료명만 빨간색 글씨로 표시한다. 레시피 자체는 숨기거나 제외하지 않고 그대로 노출한다 (참고용으로 보여주되 부족한 부분만 경고)
- **극단적 케이스**: 레시피 전체 재료 중 과반 이상이 없는 재료인 경우에도 별도 필터링 없이 동일하게 빨간색 표시만 하고 노출한다 (추천 개수 3개 이하 유지가 우선)

### 5-4. AI 응답 지연 (타임아웃 전 단계)
- **발생 조건**: 요약 버튼 클릭 후 AI 응답이 일정 시간(예: 5~10초) 이상 지연되는 경우
- **화면 반응**: 요약 버튼 또는 결과 영역에 로딩 인디케이터를 계속 표시한다. 일정 시간(예: 15~20초)을 초과하면 "응답이 지연되고 있습니다. 잠시만 기다려주세요" 등의 안내 문구를 추가로 노출한다
- **사용자 행동**: 별도 취소 버튼 없이 대기하거나, 화면을 벗어나면 요청은 백그라운드에서 종료 처리한다

### 5-5. AI 응답 실패 (최종 타임아웃 / 에러 응답)
- **발생 조건**: AI 호출이 명시적 에러를 반환하거나, 최대 대기시간(예: 30초)을 초과한 경우
- **화면 반응**: 로딩 표시를 제거하고 결과 영역에 "레시피를 불러오지 못했습니다. 다시 시도해주세요" 안내 문구와 함께 재시도 버튼을 표시한다
- **화면 안정성**: 입력 영역(첨부한 사진, 선택한 조미료)은 초기화되지 않고 그대로 유지되어, 재시도 버튼 클릭 시 동일한 입력값으로 다시 요청할 수 있다
- **금지 사항**: 에러 발생 시 화면이 하얗게 뜨거나(백지 화면), 브라우저/앱이 멈추는 현상이 없어야 한다

### 5-6. 사진 미첨부 상태로 요약 버튼 클릭
- **발생 조건**: 사진을 첨부하지 않고 요약 버튼을 클릭한 경우
- **화면 반응**: "식재료 사진을 찍으세요" 안내 문구를 강조 표시하고, AI 호출 자체를 시도하지 않는다 (불필요한 요청 방지)

---

## 6. 완료 조건

- [ ] 사진을 첨부하고 조미료를 선택한 뒤 요약 버튼을 클릭하면 결과 영역에 레시피 1~3개가 표시된다
- [ ] 사진 인식에 실패하면 "재촬영" 문구가 빨간색으로 표시되고 다음 단계로 진행되지 않는다
- [ ] 요약 버튼 클릭 후 AI 응답 대기 중에는 로딩 표시가 화면에 보인다
- [ ] AI 응답이 실패하거나 과도하게 지연되어도 화면이 멈추거나 깨지지 않고, 재시도 가능한 안내가 표시된다
- [ ] 추천된 레시피에 사용자가 보유하지 않은 재료가 있으면 해당 재료가 빨간색 글씨로 구분 표시된다
- [ ] 사진 미첨부 또는 조미료 미선택 상태에서는 요약 버튼 클릭 시 결과 영역으로 진행되지 않는다